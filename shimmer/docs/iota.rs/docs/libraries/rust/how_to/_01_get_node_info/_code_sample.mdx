import DotEnvWarning from '../../../../_admonitions/_dot_env_warning.md';

<DotEnvWarning />

```rust reference
https://github.com/iotaledger/iota.rs/tree/develop/client/examples/node_api_core/02_get_info.rs
```

### Run the Example

Run the example by running the following command:

```bash
cargo run --example node_api_core_get_info --release -- https://api.testnet.shimmer.network
```

- You can replace `https://api.testnet.shimmer.network` with any node url.
