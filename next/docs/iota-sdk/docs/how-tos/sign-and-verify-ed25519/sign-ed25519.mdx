---
title: Sign Ed25519
description: 'How to sign with Ed25519'
image: /img/logo/iota_mark_light.png
keywords:
  - how to
  - sign
  - ed25519
  - nodejs
  - python
  - rust
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

[Ed25519](https://ed25519.cr.yp.to/) is a widely adopted digital signature algorithm known for its strong security and
efficiency. Signing with Ed25519 ensures data integrity, authenticity, and non-repudiation. When signing with Ed25519,
a private key generates a digital signature that can be verified with the corresponding public key. This
process guarantees that the data has not been tampered with and originated from the private key's holder.

## Code Example

<Tabs groupId="language" queryString>
<TabItem value="rust" label="Rust">

The following example will:

1. Create a [`StrongholdSecretManager`](https://docs.rs/iota-sdk/latest/iota_sdk/client/secret/stronghold/type.StrongholdSecretManager.html) with the `STRONGHOLD_PASSWORD` and `MNEMONIC` variables in your `.env` file.
2. Use the [`SecretManager`](https://docs.rs/iota-sdk/latest/iota_sdk/client/secret/enum.SecretManager.html) to sign the `FOUNDRY_METADATA` message as bytes using the [`StrongholdAdapter.sign_ed25519()`](https://docs.rs/iota-sdk/latest/iota_sdk/client/stronghold/struct.StrongholdAdapter.html#method.sign_ed25519) function.
3. Print the signature's public key and the signature as bytes.
4. Hash the public key to get the address using the [`hex_public_key_to_bech32_address()`](https://docs.rs/iota-sdk/latest/iota_sdk/client/utils/fn.hex_public_key_to_bech32_address.html) function.

```rust reference
https://github.com/iotaledger/iota-sdk/blob/develop/sdk/examples/how_tos/sign_and_verify_ed25519/sign_ed25519.rs
```

</TabItem>
<TabItem value="typescript-node" label="Typescript (Node.js)">

The following example will:

1. Create a [`StrongholdSecretManager`](https://wiki.iota.org/next/iota-sdk/references/nodejs/interfaces/StrongholdSecretManager/) with the `STRONGHOLD_PASSWORD` and `MNEMONIC` variables in your `.env` file.
2. Use the [`SecretManager`](https://wiki.iota.org/next/iota-sdk/references/nodejs/classes/SecretManager/) to sign the `FOUNDRY_METADATA` message as bytes using the [`SecretManager.signEd25519()`](https://wiki.iota.org/next/iota-sdk/references/nodejs/classes/SecretManager/#signed25519) function.
3. Print the signature's public key and the signature as bytes.
4. Hash the public key to get the address using the [`Utils.hexPublicKeyToBech32Address()`](https://wiki.iota.org/next/iota-sdk/references/nodejs/classes/Utils/#hexpublickeytobech32address) function.

```typescript reference
https://github.com/iotaledger/iota-sdk/blob/develop/bindings/nodejs/examples/how_tos/sign_and_verify_ed25519/sign-ed25519.ts
```

</TabItem>
<TabItem value="python" label="Python">

The following example will:

1. Create a [`StrongholdSecretManager`](https://wiki.iota.org/next/iota-sdk/references/python/iota_sdk/secret_manager/#strongholdsecretmanager-objects) with the `STRONGHOLD_PASSWORD` and `MNEMONIC` variables in your `.env` file.
2. Use the [`SecretManager`](https://wiki.iota.org/next/iota-sdk/references/python/iota_sdk/secret_manager/) to sign the `FOUNDRY_METADATA` message as bytes using the [`SecretManager.sign_ed25519()`](https://wiki.iota.org/next/iota-sdk/references/python/iota_sdk/secret_manager/#sign_ed25519) function.
3. Print the signature's public key and the signature as bytes.
4. Hash the public key to get the address using the [`Utils.hex_public_key_to_bech32_address()`](https://wiki.iota.org/next/iota-sdk/references/python/iota_sdk/utils/#hex_public_key_to_bech32_address) function.

```python reference
https://github.com/iotaledger/iota-sdk/blob/develop/bindings/python/examples/how_tos/sign_and_verify_ed25519/sign_ed25519.py
```

</TabItem>
</Tabs>

## Expected Output

```plaintext
Public key: 0x67b7fc3f78763c9394fc4fcdb52cf3a973b6e064bdc3defb40a6cb2c880e6f5c
Signature: 0x5437ee671f182507103c6ae2f6649083475019f2cc372e674be164577dd123edd7a76291ba88732bbe1fae39688b50a3678bce05c9ef32c9494b3968f4f07a01
Address: rms1qpllaj0pyveqfkwxmnngz2c488hfdtmfrj3wfkgxtk4gtyrax0jaxzt70zy
```
